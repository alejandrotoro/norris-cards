= form_for @caracteristica_cartum do |f|
  -if @caracteristica_cartum.errors.any?
    #error_explanation
      %h2= "#{pluralize(@caracteristica_cartum.errors.count, "error")} prohibited this caracteristica_cartum from being saved:"
      %ul
        - @caracteristica_cartum.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :carta_id
    = f.number_field :carta_id
  .field
    = f.label :caracterisitca_id
    = f.number_field :caracterisitca_id
  .field
    = f.label :valor
    = f.text_field :valor
  .actions
    = f.submit 'Save'
